# FridgeSnap

iOS app: photograph your fridge, photo recognition identifies the ingredients, pick a laziness level (Lazy AF / Some Effort / Chef Mode), get 3 recipes with calories and macros. $4.99/month subscription, 7-day trial, 3 lifetime free scans. Full product spec: `lazychef-spec.md` (canonical JSON schemas in sections 4 and 5).

The name FridgeSnap is FINAL (2026-07-09). Domain: usefridgesnap.com. Bundle id `com.usefridgesnap.app`. Pricing (owner-set 2026-07-09): $4.99/month AND $40/year, both with 7-day trial; product ids `fridgesnap_monthly_499` + annual id to define in M4. Support email: info@prrenovatio.com. Apple Developer / App Store Connect / RevenueCat setup is now unblocked. The product spec keeps its historical filename `lazychef-spec.md`; the GitHub repo was renamed to erubero/fridgesnap and the local folder to ~/Developer/Sppa/Fridgesnap (2026-07-10). Landing FAQ section was removed at owner request 2026-07-09; do not re-add.

## Layout

- `project.yml`: XcodeGen source of truth. The `.xcodeproj` is a generated artifact; never edit the pbxproj by hand. Regenerate: `xcodegen generate` (Terminal, repo root).
- `ios/FridgeSnap/`: SwiftUI app (iOS 17+, iPhone only, portrait, MVVM, SwiftData local persistence).
- `ios/FridgeSnapTests/`: XCTest. Business logic stays Foundation-only so it runs in Simulator.
- `ios/Config/`: `Shared.xcconfig` (committed, empty keys) includes gitignored `Secrets.xcconfig` (team id, Supabase URL/anon key, RevenueCat public key). Copy `Secrets.xcconfig.example` to start.
- `supabase/migrations/`: SQL, `YYYYMMDDNNNNNN_snake_case.sql`, lowercase keywords, split by concern.
- `supabase/functions/`: Deno edge functions. `scan` and `generate` proxy the Anthropic API; the key lives only in Supabase secrets, never in the app.
- `brand/`: official logo (brand/logo/fridgesnap-logo.png, owner-supplied 2026-07-10) + brand guide README (tokens, voice rules) + animations folder. Icons derive from the logo; web copies live in landing/assets, app icon in ios/FridgeSnap/Assets.xcassets.
- `landing/`: static landing page (plain HTML/CSS). Deployed as Cloudflare Workers static assets via `wrangler.jsonc`; the repo is connected to Cloudflare Workers Builds, so push to main = deploy. Manual: `npx wrangler deploy` (Terminal, repo root).
- `.github/workflows/ci.yml`: builds and tests the iOS app on every push and PR.
- `scripts/`: admin/one-off scripts (Chef's Picks import in M5).

## Commands

- Generate project: `xcodegen generate` (Terminal, repo root)
- Build keyless: `xcodebuild build -scheme FridgeSnap -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
- Tests: `xcodebuild test -scheme FridgeSnap -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`

## Rules

- Claude writes SQL migrations and edge functions only. The OWNER runs `supabase db push`, `supabase functions deploy`, and `supabase secrets set` in Terminal. Owner instructions are numbered lists, one action per step, always saying where the command runs (Terminal vs Browser vs Xcode).
- Never upload to App Store Connect or TestFlight without explicit owner approval.
- "Commit" means commit AND push to GitHub; only when the owner asks.
- Every remote dependency sits behind a protocol (`ScanServicing`, `GenerationServicing`, `PurchasesServicing`, `AuthServicing`) with a mock. The app must always build and run end to end in Simulator with no `Secrets.xcconfig` (`AppConfig.isConfigured` selects mocks).
- Only `PurchasesService.swift` may touch `Purchases.shared` (RevenueCat), same as WindDown.
- Copy rules: no em dashes anywhere (code comments included). Never say "AI" in App Store, landing, or marketing copy; say "photo recognition". Nutrition values are always labeled "Estimated". Headlines state literally what the product does.
- Server is source of truth for: lifetime scan count (free-tier gate must survive reinstall), subscription entitlement (re-verified server-side via RevenueCat REST), community content. SwiftData holds scan history cache, saved recipes, prefs.

## Risk register

1. App Review UGC (guideline 1.2): report AND block-user AND moderation must all demonstrably work before submission. `blocked_users` is a first-class table; the feed excludes blocked authors. Auto-unpublish at 3 reports now implemented (`reports_auto_unpublish` trigger, 20260709000004).
2. Privacy label says scan photos are deleted after 24h. That is only true while the hourly `cleanup-scan-images` cron runs. Re-verify before submission: query for scans past `expires_at` that still have storage objects.
3. Vision JSON reliability: forced tool-use schema, server-side validation, one auto-retry; client treats a malformed scan as retryable, never a crash.
4. StoreKit sandbox quirks: use the StoreKit configuration file in Simulator, a sandbox Apple ID on device; entitlement truth is the server `subscriptions` row.
5. API cost: 20 scans/day, 5 regenerations/scan, 24h scan cache keyed by image-set hash.
6. RLS is the only thing standing between an authenticated user and every other user's data; every new table/policy needs the same review this pass got (own-row check on every verb, referenced-row visibility on every FK the client can set, column-level protection for anything privileged). See migration 20260709000004 for the pattern.

## Milestone ledger

- [x] M1 scaffold + backend (this session, 2026-07-09): repo scaffold, migrations, `scan`/`generate`/`cleanup-scan-images` functions. Owner setup steps pending (Supabase project, secrets, deploys).
- [x] M2 core loop (2026-07-09): Sign in with Apple (hashed nonce, gated to real backend), camera + PhotosPicker capture, ImagePipeline (1568px JPEG 0.8), ingredient review (? chips, one-tap remove, manual add, use-soon badges), scan history (SwiftData, last 10), laziness selector, results + regenerate, recipe detail. Rescue-aware generation: perishability_days flows to /generate and the prompt prefers expiring ingredients. 16 tests green. Real-device E2E pending owner setup (Supabase + Apple App ID, blocked on final name).
- [x] Rename to FridgeSnap (2026-07-09): bundle com.usefridgesnap.app, product fridgesnap_monthly_499, worker "fridgesnap", all copy. GitHub repo renamed to erubero/fridgesnap 2026-07-10.
- [x] Freshness upgrade (2026-07-09, FruitCue concepts): scan schema + prompt add ripeness (very_firm/slightly_firm/ready/very_soft/spoiled/not_applicable, judged from VISIBLE state) and storage_tip; client shows due labels ("use today" / "due Friday") anchored to scan date, eat-me-first sorting, semantic freshness colors (amber use-soon, crimson spoiled, separate from brand red). Old scans without ripeness still decode (optional fields).
- [x] SnapFridge.dc design implemented (2026-07-09): owner's claude.ai/design project ("Fridge-to-recipe AI app", file SnapFridge.dc.html) is the design source of truth. Landing rebuilt to artboard 1a (cream #FAFAF7 canvas, green #1FA24A brand, Sora + Plus Jakarta Sans, glass sticky nav, drifting scroll-parallax blobs, dark how-it-works band, amber/green/purple effort cards, dark CTA). iOS restyled to artboard 1c via Theme.swift tokens: green tint, LevelBadge per-mode colors, design copy on the effort selector, dark MacroPanel with green kcal. Copy substitutions applied per standing rules: no "AI", no fake stats (dropped "40k dinners rescued"), no Android button, name FridgeSnap not SnapFridge. Nutrition schema gained fiber_g/sugar_g (optional client-side for old recipes). Artboard 1b (web app) is out of scope.
- [x] M3 Cook Mode, post-cook sheet, My Recipes, analytics events (2026-07-09): `CookModeModel` (Foundation-only step/timer state machine) + `CookModeView` (full-screen, huge text, swipe or tap-anywhere to advance, tappable per-step timer, idle-timer disabled for the whole session). "Start cooking" on the recipe detail screen launches it; finishing the last step shows `PostCookSheetView` (1-5 star rating, private notes, save toggle). `SavedRecipe` SwiftData model (local-only cache, mirrors LocalScan's pattern; server-side recipe_saves/recipe_cooks stay for M5 community publishing). `MyRecipesView` replaces the tab placeholder: sort by recent/most cooked/top rated, swipe to delete, taps into recipe detail. Recipe detail also got a heart button to save without cooking. `AnalyticsServicing` (Supabase + mock) logs scan_completed, ingredients_edited, level_selected, recipe_generated/regenerated, cook_started, cook_completed, recipe_saved into analytics_events (existing RLS policy requires user_id = auth.uid(), now set explicitly on insert). 32 tests green (10 new). Verified via `xcodebuild test` and a Simulator launch (tab bar renders correctly, no crash); no interactive tap-through since this sandbox has no Accessibility permission for Simulator UI automation, so Cook Mode/post-cook/My Recipes should get a manual pass on a real run.
- [x] Onboarding (2026-07-09, pulled forward from M4): first-run flow gated by `hasCompletedOnboarding` in `@AppStorage`, ahead of the sign-in check in RootTabView. Order: welcome -> funny quiz (4 questions: know how to cook, care to learn, weight loss interest, delivery frequency; answers kept in-memory for now, not yet synced or used to tailor recipes) -> account creation (embeds Sign in with Apple via extracted `AppleSignInButton`, mocked with a plain "Create account" button when `services.isUsingMocks` so the Simulator flow never needs a real Apple ID) -> dietary prefs (multi-select + free-text allergies) -> staples yes/no -> trial mention ("7 days free") -> `ProfileServicing.updatePreferences` writes dietary_prefs/staples to the existing `profiles` row (RLS "update own profile" policy already covered this, no new migration). Progress bar is intentionally absent during welcome/quiz and only appears from the account step onward, landing on 25% the instant it's shown rather than animating up from 0% (endowed-progress effect, per owner request) -> 50% dietary -> 75% staples -> 100% trial. `SignInView` is now only the sign-out fallback. 7 new tests (`OnboardingModelTests`) cover the checkpoint math and quiz/back navigation; 39 tests total green. Verified with `xcodebuild test` plus a fresh-install Simulator launch showing the welcome screen; no full tap-through (same Accessibility-permission limitation as M3) so give the quiz -> account -> dietary -> staples -> trial chain a manual run once.
- [x] RLS security hardening (2026-07-09): review of the M1 schema/policies found a live privilege-escalation hole -- the `profiles` "update own profile" policy checked row ownership but not which columns changed, so any signed-in user could `update profiles set is_admin = true where id = auth.uid()`. Closed with a `security definer` BEFORE UPDATE trigger (`protect_profile_privileges`) that pins `is_admin` back unless the write comes from `service_role`, the standard Supabase column-protection pattern. Also closed: `recipe_saves`/`recipe_cooks`/`reports` insert (and cooks update) policies now require the referenced recipe be actually visible to the caller, not just that `user_id = auth.uid()` -- previously a client that learned any recipe UUID could save/cook/report against it regardless of visibility. Added the missing auto-unpublish-at-3-reports trigger (risk register item 1 was flagging this as required-before-submission and it was never built). Added a self-block table constraint, an analytics_events event-name allow-list + payload size cap, length/range constraints on recipes/reports/recipe_cooks text and numeric fields, a handle-format + dietary_prefs-shape constraint on profiles, and file-size/MIME-type limits on both storage buckets (previously unlimited). Also fixed a non-constant-time `CRON_SECRET` comparison in `cleanup-scan-images` (timing side-channel). All additive/tightening only, in migration `20260709000004_rls_hardening.sql`; nothing here changes what the current app (mocks-only, no live Supabase project yet) actually exercises, so no client changes were needed. Not dry-run against a live database (no Docker/local Supabase project in this environment) -- reviewed carefully against the existing migrations' exact policy names and conventions instead; owner should skim the migration before `supabase db push`, and needs to redeploy `cleanup-scan-images` for the CRON_SECRET fix to take effect.
- [ ] M4 monetization: paywall, gating, rc-sync. Blocked on final app name.
- [ ] M5 community: feed, publish + moderation, report/block, Chef's Picks, nightly popular_combos.
- [ ] M6 settings, delete-account, landing polish, TestFlight prep. (Landing page, wrangler.jsonc, README, and CI were pulled forward and done 2026-07-09.)
